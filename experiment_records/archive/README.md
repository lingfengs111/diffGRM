# Shared archive

This directory contains terminal, immutable experiment records that have been
reviewed and merged. Every machine reads it; no experiment process writes here
directly. Promote a completed, failed, or invalid machine-owned record with
`scripts/archive_experiment_records.py`.
